import { AppSelectValidatorDirective } from './app-select-validator.directive';

describe('AppSelectValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSelectValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
